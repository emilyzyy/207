<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>closeai</groupId>
    <artifactId>closeai-trip-planner</artifactId>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>11</maven.compiler.release>
        <junit.version>5.12.2</junit.version>
        <!--
            0.8.13 or newer is required here: this repository is built and run on Java 24,
            whose class files are major version 68, and 0.8.12's bundled ASM refuses to read
            them. The agent still instruments the project's own classes (compiled to release
            11) correctly, so coverage figures were never wrong, but every JDK class it
            touched raised IllegalClassFormatException and buried the build output in
            hundreds of stack traces.
        -->
        <jacoco.version>0.8.13</jacoco.version>
        <checkstyle.plugin.version>3.6.0</checkstyle.plugin.version>
        <checkstyle.tool.version>10.21.4</checkstyle.tool.version>
        <jackson.version>2.18.4</jackson.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.0</version>
                <configuration>
                    <release>11</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.3</version>
            </plugin>
            <!--
                Coverage evidence. Reports only: the build is not failed on a threshold,
                because much of the repository is other members' in-progress work and a
                gate here would block the whole team rather than improve anything.
                Run: ./mvnw clean test  ->  target/site/jacoco/index.html
            -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <configuration>
                    <excludes>
                        <!-- Swing rendering: exercised by hand, not meaningfully unit-testable. -->
                        <exclude>closeai/adapters/views/**</exclude>
                        <!-- Opt-in live adapters: covered by tests that require network access. -->
                        <exclude>closeai/infrastructure/web/**</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--
                Style evidence (Piazza @275). Reports only, for the same reason as above.
                Run: ./mvnw checkstyle:check  ->  target/checkstyle-result.xml
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${checkstyle.plugin.version}</version>
                <!--
                    The plugin ships with an older Checkstyle than the course configuration
                    needs: config/mystyle.xml uses SuppressWithNearbyTextFilter and
                    NoCodeInFile, added in Checkstyle 10.10 and 10.9. Pinning the tool here
                    is what lets the course file be used exactly as distributed, rather than
                    editing the course's rules to suit our build.
                -->
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>${checkstyle.tool.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <configLocation>config/mystyle.xml</configLocation>
                    <includeTestSourceDirectory>true</includeTestSourceDirectory>
                    <consoleOutput>true</consoleOutput>
                    <failOnViolation>false</failOnViolation>
                    <violationSeverity>error</violationSeverity>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.6.3</version>
                <configuration>
                    <mainClass>trippy.Main</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
